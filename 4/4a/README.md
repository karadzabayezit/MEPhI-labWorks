# To compile and run programm fust hit the following command in command line:

### Use python(recommended):
## ```python run.py```

### For bash:
## ```bash run.sh```

# Run the command for each programm (4a & 4b).
